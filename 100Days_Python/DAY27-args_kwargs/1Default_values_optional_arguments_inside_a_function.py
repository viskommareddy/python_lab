"""
# keyword arguments

def my_function(a,b,c):
    # do this with a
    # then do this with b
    # finally do this with c
    
my_function(c=3,a=1,b=2)

# arguments with default values

def my_function(a=1,b=2,c=3):
    # do this with a
    # then do this with b
    # finally do this with c
    
my_function()

# if we want to change the default value for 'b' alone, we can do so while calling the function

my_function(b=5) #this doesn't change the other arguments a,c but b only

#if an argument is not assigned with any value as default, we can assign it while calling the function

def my_function(a,b=3,c=6):

    dfngg
    kjfdbirjgb
    kjf

my_function(4) # this assigns the value 4 to the a

myfunction(4,5) # this assigns the value 4 to a and changes the value of b to 5

"""

